## \sql - School project (Oracle SQL)

The sql/ directory contains a complete set of Oracle SQL scripts prepared for the Database Systems course project.
These scripts are not part of the runtime application, they are provided for demonstration of Oracle/PL‑SQL skills.

 - `create_tables.sql`		- creates tables, seqences and defines referential intengrity constraints
 - `insert_data.sql`		- inserts sample data into tables
 - `triggers_update.sql`	- two triggers, procedure, cursor, recalculates statistics table
 - `select.sql`			- several select statements
